# Money — MVP учета договоров/расходов

Backend-сервис на FastAPI + PostgreSQL.

## Запуск (Docker)

```bash
docker compose up --build
```

После старта:
- API: `http://localhost:8000`
- Swagger: `http://localhost:8000/docs`

## Миграции

Контейнер приложения сам применяет миграции при старте.

